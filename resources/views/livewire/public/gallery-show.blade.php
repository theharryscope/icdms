<div class="bg-canvas text-ink min-h-screen font-sans selection:bg-ochre selection:text-canvas">
    <x-public.site-header />
    <section class="py-20"><div class="max-w-6xl mx-auto px-6"><div class="max-w-3xl mb-12"><span class="text-[11px] font-mono font-semibold uppercase tracking-widest text-ochre">{{ $album->category }}</span><h1 class="text-3xl md:text-4xl font-display font-bold text-ink tracking-tight mt-3">{{ $album->title }}</h1>@if($album->description)<p class="text-sm text-ink-muted leading-relaxed mt-4">{{ $album->description }}</p>@endif<span class="block text-[10px] font-mono text-ink-muted uppercase tracking-wider mt-4">{{ $album->published_at?->format('F j, Y') }} · {{ $album->images->count() }} photos</span></div><div class="columns-1 sm:columns-2 lg:columns-3 gap-5 space-y-5">@foreach($album->images as $image)<figure class="break-inside-avoid bg-surface border border-line rounded-xl overflow-hidden"><img src="{{ $image->image_url }}" class="w-full object-cover" alt="{{ $image->caption ?: $album->title }}">@if($image->caption)<figcaption class="p-3 text-xs text-ink-muted">{{ $image->caption }}</figcaption>@endif</figure>@endforeach</div><div class="mt-14 pt-6 border-t border-line"><a href="{{ route('public.gallery') }}" class="text-xs font-bold text-ochre hover:text-ochre/80">&larr; Back to Gallery</a></div></div></section>
    <x-public.site-footer />
</div>
